/**
 * Rendert den Hauptcontainer für alle Overlay-Elemente.
 * @returns {string} Der HTML-String für den Overlay-Container.
 */
function renderOverlay() {
  return `
    <div id="overlay-container"></div>
  `;
}
